/*


import React from 'react';
import baldgoofy from './quizImages/baldgoofy.png'; // Import the image

const ImageDisplay = ({ quizID, currentQuestionID }) => {
    console.log("quizID:", quizID); // Log the quizID
    console.log("currentQuestionID:", currentQuestionID); // Log the currentQuestionID
  
    if (quizID === "Introduction" && currentQuestionID === 16) {
      return (
        <img
          src={baldgoofy}
          alt="Bald Goofy"
          style={{
            width: "300px",
            height: "300px",
            objectFit: "contain",
            borderRadius: "8px",
          }}
        />
      );
    }
  
    return null;
  };
  

export default ImageDisplay;

*/